dependencies:
  pre:
    - sudo apt-get install libav-tools
    - sudo apt-get install -y build-essential
    - npm install -g npm

machine:
  node:
    version: 7.9.0

general:
  branches:
    only:
      - deploy